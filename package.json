{
  "name": "simple-park-pages",
  "version": "1.0.0",
  "description": "Public static pages (home + privacy policy) for Simple Park",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i src/input.css -o css/styles.css --minify",
    "watch": "tailwindcss -i src/input.css -o css/styles.css --watch"
  },
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
